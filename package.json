{
  "name": "simple-cv",
  "version": "0.1.2",
  "description": "Node.js bindings for OpenCV with a simple promise-based async API, good documentation and excellent tests.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --slow 10 --timeout 10000 --reporter spec tests.js",
    "test-show": "env SHOW_IMAGES=true mocha --slow 10 --timeout 10000 --reporter spec tests.js"
  },
  "author": "Sami Koskimäki",
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.5.1"
  },
  "devDependencies": {
    "cubic-spline": "^1.0.4",
    "expect.js": "^0.3.1",
    "lodash": "^4.17.4",
    "mocha": "^3.2.0"
  }
}
